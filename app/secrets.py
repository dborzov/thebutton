"""
    In production the access keys must be stored separately. 
    One simple way would to store them in ENV variables and fetch them here with os.getenv
"""

SECRET_KEY = 'Well, that is not a very secret key'